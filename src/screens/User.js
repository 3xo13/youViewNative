import React from 'react'
import {
	SafeAreaView,
	FlatList,
	StyleSheet,
	ImageBackground,
	View,
	Text
} from 'react-native'

const User = ({ weatherData }) => {
	return (
		<View>
			<Text>user</Text>
		</View>
	)
}

const pageStyles = StyleSheet.create({})

export default User
